# src/api

接口请求层，所有后端 API 统一管理。

## 存放内容
- `request.ts`：Axios 实例封装（请求/响应拦截器、Token 注入、统一错误处理、超时配置）
- `modules/`：按业务模块拆分的接口文件

## 设计原则
- 组件中禁止直接调用 axios，必须通过本层接口函数发起请求
- 每个接口函数必须定义完整的 TypeScript 类型（请求参数、响应数据）
- 后端接口变更只修改本层，不影响业务组件
