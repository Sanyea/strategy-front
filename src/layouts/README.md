# src/layouts

布局组件目录，定义页面整体框架结构。

## 存放内容
- `DefaultLayout.vue`：主布局（侧边栏 + 顶部导航 + 内容区 + 标签页）
- `BlankLayout.vue`：空白布局（登录页、全屏页使用）

## 设计原则
- 布局组件只负责结构框架，不包含业务逻辑
- 通过 `<router-view />` 渲染子页面
- 不同布局通过路由 meta 配置切换
