# src/router

路由配置目录，管理应用的页面路由体系。

## 存放内容
- `index.ts`：路由实例创建、全局守卫（登录校验、权限判断、进度条）
- `modules/`：按业务模块拆分的路由配置

## 核心职责
- 定义路由表（路径、组件、meta 信息）
- 全局前置守卫：未登录跳转登录页
- 权限守卫：根据用户角色过滤可访问路由
- 动态路由：根据后端返回的菜单动态添加路由
