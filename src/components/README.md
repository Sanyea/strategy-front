# src/components

全局通用组件目录，全项目可复用。

## 子目录
- `base/`：基础 UI 组件（对 UI 库的二次封装，与业务无关）
- `business/`：通用业务组件（多个页面复用的业务组件）

## 组件归属判断
- 多个模块都会用到 → 放在这里
- 仅单个页面/模块使用 → 放在对应 `views/xxx/components/`
- 禁止把页面独有的组件放到全局，避免组件泛滥
